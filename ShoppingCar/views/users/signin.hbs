<div class="row">
    <div class="col-md-4 col-md-offset-4">
        <h1> Sigin <h1>
            {{!-- 模板引擎handle-bar的语法 hasErrors messages是外部通过 render 传进来的值  --}}
            {{#if hasErrors}}
                <div class="alert alert-danger">
                    {{# each messages }}
                    {{!-- this 表示 遍历 messages 数组中的一个--}}
                         <p>{{this}}</p> 
                    {{/each}}
                </div>
            {{/if }}
        <form action="/user/signin" method="post">
            <div class="form-group">
                <label for="email">E-mail</label>
                <input type="text" id = "email" name="email" class="form-contrl">
            </div>
            <div class="from-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="password" class="form-contrl">
            </div>
            <input type="hidden" name="_csrf" value="{{ csrfToken }}">
            <button type="submit" class="btn btn_primary">signin</button>
        </form>
    </div>
</div>